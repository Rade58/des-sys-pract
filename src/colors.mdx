import { Meta, ColorPalette, ColorItem, } from '@storybook/addon-docs/blocks'

import { colors } from './tokens/colors'

<Meta title="Tokens/Colors" />

# There is more you can do here besides displaying palette

There are also Components you can use to display typography and icons:
  `IconGalery` and `Typeset`

# Here are Colors


<ColorPalette>
{Object.entries(colors).map(([k,v]) => (<ColorItem subtitle={k} key={k} colors={v} />))}
</ColorPalette>